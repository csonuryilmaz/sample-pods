# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'sample-pods' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Eureka'
  pod 'SwiftyButton',  :git => 'https://github.com/tosbaha/SwiftyButton.git'

  target 'sample-podsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'sample-podsUITests' do
    # Pods for testing
  end

end
